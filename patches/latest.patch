--- a/src/renderer/src/ui/App.tsx
+++ b/src/renderer/src/ui/App.tsx
@@
-import { ipcRenderer } from 'electron';
+// Electron IPC is only available in the renderer process.
+// In test environments (Node) this module is not available,
+// so we provide a graceful fallback.
+let ipcRenderer: any = null;
+try {
+  // @ts-ignore
+  ipcRenderer = window.require ? window.require('electron').ipcRenderer : null;
+} catch (e) {
+  ipcRenderer = null;
+}
@@
 // rest of the file remains unchanged
--- a/src/renderer/src/ui/pages/ProductsPage.tsx
+++ b/src/renderer/src/ui/pages/ProductsPage.tsx
@@
-import { ipcRenderer } from 'electron';
+// Electron IPC is only available in the renderer process.
+// In test environments (Node) this module is not available,
+// so we provide a graceful fallback.
+let ipcRenderer: any = null;
+try {
+  // @ts-ignore
+  ipcRenderer = window.require ? window.require('electron').ipcRenderer : null;
+} catch (e) {
+  ipcRenderer = null;
+}
@@
 // rest of the file remains unchanged
--- a/src/renderer/src/ui/pages/LogsPage.tsx
+++ b/src/renderer/src/ui/pages/LogsPage.tsx
@@
-import { ipcRenderer } from 'electron';
+// Electron IPC is only available in the renderer process.
+// In test environments (Node) this module is not available,
+// so we provide a graceful fallback.
+let ipcRenderer: any = null;
+try {
+  // @ts-ignore
+  ipcRenderer = window.require ? window.require('electron').ipcRenderer : null;
+} catch (e) {
+  ipcRenderer = null;
+}
@@
 // rest of the file remains unchanged
--- a/src/renderer/src/ui/pages/SettingsPage.tsx
+++ b/src/renderer/src/ui/pages/SettingsPage.tsx
@@
-import { ipcRenderer } from 'electron';
+// Electron IPC is only available in the renderer process.
+// In test environments (Node) this module is not available,
+// so we provide a graceful fallback.
+let ipcRenderer: any = null;
+try {
+  // @ts-ignore
+  ipcRenderer = window.require ? window.require('electron').ipcRenderer : null;
+} catch (e) {
+  ipcRenderer = null;
+}
@@
 // rest of the file remains unchanged
