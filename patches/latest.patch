--- a/electron/main/index.ts
+++ b/electron/main/index.ts
@@ -14,6 +14,18 @@
     .toLowerCase()
 
   return raw === '1' || raw === 'true' || raw === 'yes' || raw === 'on'
+}
+
+function getActiveStoreClientIdOrNull(): string | null {
+  try {
+    const fromSecrets = String(loadSecrets().clientId ?? '').trim()
+    if (fromSecrets) return fromSecrets
+  } catch {
+    // Без ключей UI всё равно может работать в seed-режиме.
+  }
+
+  if (isUiTestSeedEnabled()) return UI_TEST_SEED_DEFAULT_STORE_ID
+  return null
 }
 
 function makeUiTestSeedProducts(storeClientId: string | null, count = 1200) {
@@ -48,14 +60,7 @@
 function seedUiTestDataIfEnabled() {
   if (!isUiTestSeedEnabled()) return
 
-  let storeClientId: string | null = UI_TEST_SEED_DEFAULT_STORE_ID
-  try {
-    const secrets = loadSecrets()
-    const fromSecrets = String(secrets.clientId || '').trim()
-    if (fromSecrets) storeClientId = fromSecrets
-  } catch {
-    // Ключей может не быть — для UI-теста это нормально.
-  }
+  const storeClientId = getActiveStoreClientIdOrNull()
 
   try {
     const alreadyForTargetStore = dbGetProducts(storeClientId).length
@@ -336,12 +341,7 @@
 
 ipcMain.handle('data:getProducts', async () => {
   try {
-    let storeClientId: string | null = null
-    try {
-      storeClientId = loadSecrets().clientId
-    } catch {
-      storeClientId = null
-    }
+    const storeClientId = getActiveStoreClientIdOrNull()
 
     const products = dbGetProducts(storeClientId)
     return { ok: true, products }
@@ -352,12 +352,7 @@
 
 ipcMain.handle('data:getSyncLog', async () => {
   try {
-    let storeClientId: string | null = null
-    try {
-      storeClientId = loadSecrets().clientId
-    } catch {
-      storeClientId = null
-    }
+    const storeClientId = getActiveStoreClientIdOrNull()
 
     const logs = dbGetSyncLog(storeClientId)
     return { ok: true, logs }
--- a/tests/e2e/human-smoke.spec.ts
+++ b/tests/e2e/human-smoke.spec.ts
@@ -503,6 +503,13 @@
 }
 
 
+function isSeededUiScenario(): boolean {
+  const raw = String(process.env.TEST_UI_SEED ?? process.env.OZONATOR_TEST_UI_SEED ?? '')
+    .trim()
+    .toLowerCase();
+  return raw === '1' || raw === 'true' || raw === 'yes' || raw === 'on';
+}
+
 type ScrollbarGeometry = {
   ok: boolean;
   reason?: string;
@@ -870,6 +877,10 @@
   const verticalDebug = await test.step('aggressive vertical scroll (thumb-drag/wheel)', async () => {
     return assertAggressiveVerticalScrollNoBlank(page);
   });
+
+  if (isSeededUiScenario() && verticalDebug.notes.includes('vertical-scroll-not-needed')) {
+    throw new Error('FALSE_GREEN_E2E_PROXY: seeded UI scenario, но вертикальный скролл не понадобился (debug note=vertical-scroll-not-needed).');
+  }
   await test.step('horizontal scroll is always reachable', async () => {
     await assertHorizontalScrollAlwaysReachable(page);
   });
@@ -878,6 +889,10 @@
   const visibleRows = await page.locator('table tr, [role="row"], .ag-row').count().catch(() => 0);
   const visibleText = (await page.locator('body').innerText()).trim();
   expect(visibleText.length).toBeGreaterThan(0);
+
+  if (isSeededUiScenario() && /(?:^|\n|\r)\s*(?:Всего|Total)\s*:\s*0\b/i.test(visibleText)) {
+    throw new Error('FALSE_GREEN_E2E_PROXY: seeded UI scenario, но в интерфейсе отображается пустая таблица ("Всего: 0").');
+  }
 
   const finalProbe = await probePrimaryScrollable(page);
   const debugPayload = {
--- a/.github/workflows/windows-ci.yml
+++ b/.github/workflows/windows-ci.yml
@@ -198,7 +198,7 @@
             npm i -D @playwright/test --no-save --legacy-peer-deps *>&1 | Tee-Object -FilePath logs\e2e.log -Append
             if ($LASTEXITCODE -ne 0) {
               "ok=false" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
-              "status=failed: install @playwright/test" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
+              "status=failed" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
               exit 0
             }
           }
@@ -207,12 +207,14 @@
           npx playwright install chromium *>&1 | Tee-Object -FilePath logs\e2e.log -Append
           if ($LASTEXITCODE -ne 0) {
             "ok=false" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
-            "status=failed: playwright install" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
+            "status=failed" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
             exit 0
           }
 
           if ($hasHumanSmoke) {
-            "Running targeted synthetic UI test: tests/e2e/human-smoke.spec.ts" | Tee-Object -FilePath logs\e2e.log -Append
+            "Running targeted synthetic UI test: tests/e2e/human-smoke.spec.ts (TEST_UI_SEED=1)" | Tee-Object -FilePath logs\e2e.log -Append
+            $env:TEST_UI_SEED = '1'
+            $env:OZONATOR_TEST_UI_SEED = '1'
             npx playwright test tests/e2e/human-smoke.spec.ts *>&1 | Tee-Object -FilePath logs\e2e.log -Append
           } else {
             "Running default Playwright suite" | Tee-Object -FilePath logs\e2e.log -Append
@@ -225,11 +227,11 @@
             }
             "ux=PASS" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
             "ok=true" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
-            "status=ran-pass" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
+            "status=ran" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
           } else {
             "ux=FAIL" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
             "ok=false" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
-            "status=failed: playwright test" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
+            "status=failed" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
           }
 
       - name: Generate 118-matrix TestReport (auto)
@@ -240,8 +242,8 @@
           TYPECHECK_STATUS: ${{ steps.typecheck.outputs.ok == 'true' && 'PASS' || steps.typecheck.outputs.ok == 'false' && 'FAIL' || 'NR' }}
           UNIT_STATUS: ${{ steps.unit.outputs.ok == 'true' && 'PASS' || steps.unit.outputs.ok == 'false' && 'FAIL' || 'NR' }}
           BUILD_STATUS: ${{ steps.dist.outputs.ok == 'true' && 'PASS' || steps.dist.outputs.ok == 'false' && 'FAIL' || 'NR' }}
-          E2E_STATUS: ${{ (steps.e2e.outputs.status == 'ran-pass') && 'PASS' || (startsWith(steps.e2e.outputs.status, 'failed')) && 'FAIL' || 'NR' }}
-          SYNTHETIC_STATUS: ${{ (steps.e2e.outputs.status == 'ran-pass') && 'PASS' || (startsWith(steps.e2e.outputs.status, 'failed')) && 'FAIL' || 'NR' }}
+          E2E_STATUS: ${{ (steps.e2e.outputs.status == 'ran' && steps.e2e.outputs.ok == 'true') && 'PASS' || (steps.e2e.outputs.status == 'failed') && 'FAIL' || 'NR' }}
+          SYNTHETIC_STATUS: ${{ (steps.e2e.outputs.status == 'ran' && steps.e2e.outputs.ok == 'true') && 'PASS' || (steps.e2e.outputs.status == 'failed') && 'FAIL' || 'NR' }}
           VISUAL_STATUS: ${{ steps.e2e.outputs.visual || 'NR' }}
           UX_PROXY_STATUS: ${{ steps.e2e.outputs.ux || 'NR' }}
           UX_STATUS: ${{ steps.e2e.outputs.ux || 'NR' }}
