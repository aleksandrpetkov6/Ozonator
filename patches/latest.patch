diff --git a/electron/main/ozon.ts b/electron/main/ozon.ts
index 81c005c..6949b95 100644
--- a/electron/main/ozon.ts
+++ b/electron/main/ozon.ts
@@ -100,7 +100,7 @@ type ProductInfoV2 = {
   errors?: any[]
 }
 
-type AttrValue = { value?: string; dictionary_value_id?: number }
+type AttrValue = { value?: string; dictionary_value_id?: number; dictionary_value?: any; [key: string]: any }
 
 type Attribute = { id: number; name?: string; values?: AttrValue[]; [key: string]: any }
 
@@ -166,22 +166,42 @@ function normalizeBrandText(raw: any): string | null {
 }
 
 function extractBrandFromAttributes(attrs: Array<any>): string | null {
+  // Ozon бренд в attributes[] приходит по полю id (не путать с attribute_id).
+  // Типовые id:
+  // - 85: обычный бренд
+  // - 31: бренд для одежды/обуви
   const brandIds = new Set([85, 31])
 
   for (const a of attrs) {
     if (!a) continue
-    if (brandIds.has(Number((a as any).id))) {
-      const v = normalizeBrandText((a as any).values) ?? normalizeBrandText(a)
-      if (v) return v
+    if (!brandIds.has(Number((a as any).id))) continue
+
+    const values = Array.isArray((a as any).values) ? (a as any).values : []
+
+    // Основной путь для столбца «Бренд»: values[0].value (текст бренда).
+    // dictionary_value_id сохраняем как диагностическую опору (на будущее), но в UI выводим именно value.
+    for (const v of values) {
+      const rawText = (v as any)?.value
+      if (typeof rawText === 'string' && rawText.trim()) return rawText.trim()
     }
+
+    // Fallback: иногда текст может лежать глубже (dictionary_value/name и т.п.).
+    const fallback = normalizeBrandText(values) ?? normalizeBrandText(a)
+    if (fallback) return fallback
   }
 
+  // Дополнительный fallback на случай нестандартного id бренда в категории.
   for (const a of attrs) {
     const name = String((a as any)?.name ?? '').trim().toLowerCase()
     if (!name) continue
     if (name.includes('бренд') || name === 'brand') {
-      const v = normalizeBrandText((a as any).values) ?? normalizeBrandText(a)
-      if (v) return v
+      const values = Array.isArray((a as any).values) ? (a as any).values : []
+      for (const v of values) {
+        const rawText = (v as any)?.value
+        if (typeof rawText === 'string' && rawText.trim()) return rawText.trim()
+      }
+      const fallback = normalizeBrandText(values) ?? normalizeBrandText(a)
+      if (fallback) return fallback
     }
   }
 
@@ -375,15 +395,29 @@ async function fetchAttributesMap(
   const map = new Map<number, { brand?: string | null; barcode?: string | null; category?: string | null; descriptionCategoryId?: number | null; typeId?: number | null }>()
 
 
-  // Встречаются /v3/products/info/attributes и /v4/products/info/attributes.
-  // Делаем основной запрос в /v3, а /v4 используем как fallback.
+  // Для бренда/атрибутов используем метод характеристик товара.
+  // Актуальный приоритет: /v4/product/info/attributes
+  // (старые варианты /v3/... и plural-формы оставляем как fallback для совместимости).
   async function callWithFallback(body: any) {
-    try {
-      return await ozonPost(secrets, '/v3/products/info/attributes', body)
-    } catch (e: any) {
-      if (e?.details?.status !== 404) throw e
-      return await ozonPost(secrets, '/v4/products/info/attributes', body)
+    const endpoints = [
+      '/v4/product/info/attributes',
+      '/v3/product/info/attributes',
+      '/v4/products/info/attributes',
+      '/v3/products/info/attributes',
+    ]
+
+    let lastErr: any = null
+    for (const endpoint of endpoints) {
+      try {
+        return await ozonPost(secrets, endpoint, body)
+      } catch (e: any) {
+        lastErr = e
+        const st = e?.details?.status
+        if (st !== 404) throw e
+      }
     }
+
+    throw lastErr ?? new Error('Ozon attributes endpoint not available')
   }
 
   for (const pack of chunk(lookupItems, 900)) {
