diff --git a/.github/workflows/windows-ci.yml b/.github/workflows/windows-ci.yml
index 9d34ebb..b434b0e 100644
--- a/.github/workflows/windows-ci.yml
+++ b/.github/workflows/windows-ci.yml
@@ -195,9 +195,21 @@ jobs:
           $hasE2EScript = "${{ steps.detect.outputs.has_e2e_script }}" -eq "true"
 
           if (-not $hasPlaywright) {
-            "status=skipped" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
-            "Optional E2E skipped: @playwright/test is not installed, so CI remains green and E2E is marked skipped." | Set-Content -Encoding UTF8 e2e-results\e2e.log
-            exit 0
+            "Playwright dependency not found in package.json. Trying temporary CI install (@playwright/test --no-save)..." | Set-Content -Encoding UTF8 e2e-results\e2e.log
+            npm i -D @playwright/test --no-save 2>&1 | Tee-Object -FilePath e2e-results\e2e.log -Append
+            if ($LASTEXITCODE -ne 0) {
+              "status=skipped" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
+              "Optional E2E skipped: failed to install @playwright/test in CI (temporary)." | Tee-Object -FilePath e2e-results\e2e.log -Append
+              exit 0
+            }
+            node -e "require.resolve('@playwright/test/package.json')" 2>$null
+            if ($LASTEXITCODE -ne 0) {
+              "status=skipped" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
+              "Optional E2E skipped: @playwright/test still not resolvable after temporary CI install." | Tee-Object -FilePath e2e-results\e2e.log -Append
+              exit 0
+            }
+            $hasPlaywright = $true
+            "Temporary CI install of @playwright/test completed. Continuing Optional E2E." | Tee-Object -FilePath e2e-results\e2e.log -Append
           }
 
           try {
@@ -310,7 +322,7 @@ jobs:
           $lines += ""
           $lines += "Playwright dependency detected: $env:HAS_PLAYWRIGHT"
           if ($env:E2E_STATUS -eq 'skipped') {
-            $lines += "E2E note: skipped because @playwright/test is not installed in project dependencies."
+            $lines += "E2E note: skipped because @playwright/test was unavailable (or temporary CI install failed)."
           } elseif ($env:E2E_STATUS -eq 'failed') {
             $lines += "E2E note: optional step failed; see e2e-results/e2e.log. Job remains green only if required steps passed."
           } else {
