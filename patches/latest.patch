--- a/src/renderer/src/ui/pages/ProductsPage.tsx	2026-02-21 18:54:18.016410514 +0000
+++ b/src/renderer/src/ui/pages/ProductsPage.tsx	2026-02-21 18:54:18.024147893 +0000
@@ -228,7 +228,11 @@
   )
 
   const visibleSearchCols = useMemo(
-    () => cols.filter(c => c.visible).map(c => c.id),
+    () => visibleSearchKey
+      .split('|')
+      .filter(Boolean)
+      .filter((token) => token.endsWith(':1'))
+      .map((token) => token.slice(0, token.lastIndexOf(':'))),
     [visibleSearchKey]
   )
 
@@ -250,7 +254,7 @@
 
       return hay.includes(q)
     })
-  }, [products, query, visibleSearchKey])
+  }, [products, query, visibleSearchCols])
 
 
   useEffect(() => {
