diff --git a/.github/workflows/zip-ci-reports.yml b/.github/workflows/zip-ci-reports.yml
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/.github/workflows/zip-ci-reports.yml
@@ -0,0 +1,53 @@
+name: Zip CI reports (Ozonator)
+
+on:
+  workflow_run:
+    workflows: ["Windows CI (Озонатор)"]
+    types: [completed]
+
+permissions:
+  actions: read
+  contents: read
+
+jobs:
+  zip-reports:
+    if: ${{ github.event.workflow_run.conclusion == 'success' }}
+    runs-on: windows-latest
+    steps:
+      - name: Download artifacts from Windows CI run
+        uses: actions/download-artifact@v4
+        with:
+          github-token: ${{ secrets.GITHUB_TOKEN }}
+          run-id: ${{ github.event.workflow_run.id }}
+          path: _artifacts
+
+      - name: Pack TestReport + e2e-results to zip
+        shell: pwsh
+        run: |
+          $ErrorActionPreference = 'Stop'
+          New-Item -ItemType Directory -Force -Path _zips | Out-Null
+
+          $testReport = Get-ChildItem -Path _artifacts -Recurse -File -Filter 'TestReport.txt' | Select-Object -First 1
+          if ($null -ne $testReport) {
+            Compress-Archive -Path $testReport.FullName -DestinationPath (Join-Path (Get-Location) '_zips/TestReport.zip') -Force
+            Write-Host "Packed TestReport.zip from $($testReport.FullName)"
+          } else {
+            Write-Host 'TestReport.txt not found in artifacts'
+          }
+
+          $e2eDir = Get-ChildItem -Path _artifacts -Recurse -Directory | Where-Object { $_.Name -eq 'e2e-results' } | Select-Object -First 1
+          if ($null -ne $e2eDir) {
+            Compress-Archive -Path $e2eDir.FullName -DestinationPath (Join-Path (Get-Location) '_zips/e2e-results.zip') -Force
+            Write-Host "Packed e2e-results.zip from $($e2eDir.FullName)"
+          } else {
+            Write-Host 'e2e-results folder not found in artifacts'
+          }
+
+      - name: Upload zipped reports
+        if: always()
+        uses: actions/upload-artifact@v4
+        with:
+          name: ci-reports-zipped
+          path: |
+            _zips/TestReport.zip
+            _zips/e2e-results.zip
+          if-no-files-found: warn

--- a/tests/e2e/human-smoke.spec.ts
+++ b/tests/e2e/human-smoke.spec.ts
@@ -628,6 +628,16 @@
 
   const bootProbe = await waitForUiReady(page);
 
+  // Контрольная точка: оболочка интерфейса должна быть видна даже без данных
+  const uiShellPath = path.resolve('test-results', 'ui-shell.png');
+  await page.screenshot({ path: uiShellPath, fullPage: true }).catch(() => {});
+  if (fs.existsSync(uiShellPath)) {
+    await test.info().attach('ui-shell', {
+      path: uiShellPath,
+      contentType: 'image/png',
+    });
+  }
+
   // Переходы по типичным вкладкам/экранам + проверки, что экраны реально открылись
   const productsOpen1 = await clickByTexts(page, [/товар/i, /products?/i, /каталог/i], 2);
   expect(productsOpen1, 'Не удалось открыть раздел товаров/каталога').toBeGreaterThan(0);
