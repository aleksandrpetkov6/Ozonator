--- a/src/types/better-sqlite3.d.ts
+++ b/src/types/better-sqlite3.d.ts
@@
 // Minimal type definitions for better-sqlite3 to satisfy TypeScript
 // during CI builds. The real library provides a rich API, but for the
 // purposes of this project we only need a subset that is used in the
 // database module.
 
 declare module 'better-sqlite3' {
   interface Statement {
     run(...params: any[]): this;
     get(...params: any[]): any;
     all(...params: any[]): any[];
     exec(sql: string): void;
   }
 
   interface Database {
     prepare(sql: string): Statement;
     exec(sql: string): void;
     close(): void;
   }
 
   const Database: {
     new (filename: string, options?: any): Database;
   };
 
   export = Database;
 }
 
--- a/electron/main/storage/db.ts
+++ b/electron/main/storage/db.ts
@@
-import Database from 'better-sqlite3';
+// Import the Database constructor from better-sqlite3. The type definitions
+// are provided in src/types/better-sqlite3.d.ts to keep the TypeScript
+// compilation happy on CI.
+import Database from 'better-sqlite3';
 
 const dbPath = path.join(app.getPath('userData'), 'app.db');
 
 // Ensure the database file exists
@@
-const db = new Database(dbPath);
+const db = new Database(dbPath);
 
 // Example table creation
 db.exec(`
   CREATE TABLE IF NOT EXISTS settings (
     key TEXT PRIMARY KEY,
     value TEXT
   )
 `);
 
*** End of File ***
